
## 2024-07-28 - O(N*M)에서 O(N+M)으로 복잡도 개선
**Learning:** 중첩 루프 내부에서 필터를 사용할 때(O(N*M)), 두 개의 정렬된 배열(타임라인 버킷, 이벤트 로그)을 대상으로 한다면 투 포인터(Two-pointer) 접근법을 사용하여 O(N+M)의 단일 패스로 처리할 수 있습니다.
**Action:** 시계열 데이터와 같이 정렬이 보장되는 데이터 셋에 대해서는 항상 투 포인터 최적화를 고려하겠습니다.
