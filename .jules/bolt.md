## 2024-07-29 - [가상화 리스트 렌더링 성능 최적화]
**Learning:** 가상화 리스트(react-window) 내부에서 매 항목을 렌더링할 때마다 불변하는 정적 값(예: 시작 시간)을 반복적으로 Date 파싱하는 것은 큰 성능 병목이 될 수 있습니다.
**Action:** 리스트의 모든 아이템에 공통적으로 적용되는 정적 값은 렌더 루프 바깥(부모 컴포넌트의 useMemo)에서 한 번만 사전 계산하여 원시 값(primitive) 형태로 자식 Row 컴포넌트들에 전달해야 합니다.
