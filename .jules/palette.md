## 2025-02-23 - 스크린 리더 중복 읽기 방지를 위한 장식용 이미지 처리
**Learning:** 로고 이미지(`alt="Argos"`) 바로 옆에 시각적으로 동일한 텍스트(`<h2 ...>Argos</h2>`)가 렌더링될 경우, 스크린 리더가 이를 중복해서 읽어 사용자 경험을 저하시킬 수 있습니다.
**Action:** 시각적 텍스트로 이미 설명이 되는 인접한 로고(장식용) 이미지에는 `alt="" aria-hidden="true"` 속성을 동시에 부여하여 스크린 리더가 해당 요소를 건너뛰도록 처리합니다.
